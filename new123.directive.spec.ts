import { New123Directive } from './new123.directive';

describe('New123Directive', () => {
  it('should create an instance', () => {
    const directive = new New123Directive();
    expect(directive).toBeTruthy();
  });
});
